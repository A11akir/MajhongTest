using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class sasa : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("sasa start");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
